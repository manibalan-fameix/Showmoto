import type { IconSvgElement } from "@hugeicons/react"
import { CheckmarkCircle02Icon } from "@hugeicons/core-free-icons"

/** Single shared icon used for every feature item (safety, comfort, exterior, interior, infotainment) so the icon set reads as one consistent family instead of a per-feature mix. Used by both the admin feature picker and the buyer-facing Features card. */
export const featureIcon: IconSvgElement = CheckmarkCircle02Icon
